# Scrollback buffer (default is 2000, far too low)
set -g history-limit 50000

# Enable mouse support — allows scroll wheel for scrollback,
# click to select panes, and drag to resize panes
set -g mouse on

# Use 256-color terminal (matches mosh and most modern terminals)
set -g default-terminal "screen-256color"
